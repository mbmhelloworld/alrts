import ReactDOM from 'react-dom';
import React from "react";
import Fixture from "./components/matches/matches";

ReactDOM.render(
    <React.StrictMode>
        <Fixture></Fixture>
    </React.StrictMode>,
    document.getElementById('root')
);
